<Window x:Class="ReadTray.App.FloatingPlayerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="560" Height="300" MinWidth="460" MinHeight="260"
        WindowStyle="None" AllowsTransparency="True" ResizeMode="NoResize"
        Background="Transparent" ShowInTaskbar="False" Topmost="True"
        Deactivated="Window_Deactivated" KeyDown="Window_KeyDown">
    <Window.Resources>
        <Style x:Key="ToolButton" TargetType="Button">
            <Setter Property="Height" Value="34"/>
            <Setter Property="MinWidth" Value="58"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Foreground" Value="#F4F7FB"/>
            <Setter Property="Background" Value="#323A46"/>
            <Setter Property="BorderBrush" Value="#4B5563"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="8" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#414B59"/>
                                <Setter Property="BorderBrush" Value="#64748B"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2563EB"/>
                                <Setter Property="BorderBrush" Value="#60A5FA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="IconButton" BasedOn="{StaticResource ToolButton}" TargetType="Button">
            <Setter Property="Width" Value="34"/>
            <Setter Property="MinWidth" Value="34"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
            <Setter Property="FontSize" Value="16"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="18" Background="#F01F242D" BorderBrush="#334B5563" BorderThickness="1" Padding="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="46"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="82"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" CornerRadius="18,18,0,0" Background="#242B36" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid Margin="16,0,10,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="ReadTray" Foreground="White" FontWeight="SemiBold" FontSize="15" VerticalAlignment="Center"/>
                        <Border Background="#334B5563" CornerRadius="7" Padding="8,3" Margin="10,0,0,0">
                            <TextBlock x:Name="StatusText" Foreground="#C9D3E1" FontSize="12"/>
                        </Border>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Style="{StaticResource IconButton}" Content="-" ToolTip="Hide" Click="Hide_Click"/>
                        <Button Style="{StaticResource IconButton}" Content="x" ToolTip="Close player" Click="Close_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="18,14,18,8">
                <TextBox x:Name="ManualInput" Visibility="Collapsed" TextWrapping="Wrap" AcceptsReturn="True"
                         Background="#151A22" Foreground="#F4F7FB" BorderBrush="#3E4856" Padding="10"
                         VerticalScrollBarVisibility="Auto"/>
                <Border x:Name="PreviewHost" Background="#151A22" CornerRadius="10" Padding="12">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <TextBlock x:Name="PreviewText" TextWrapping="Wrap" Foreground="#EEF3FA" FontSize="14" LineHeight="20"/>
                    </ScrollViewer>
                </Border>
            </Grid>

            <Grid Grid.Row="2" Margin="18,6,18,16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <WrapPanel Grid.Row="0">
                    <Button Style="{StaticResource ToolButton}" Content="Play" Width="66" Click="Play_Click"/>
                    <Button Style="{StaticResource ToolButton}" Content="Pause" Width="72" Click="Pause_Click"/>
                    <Button Style="{StaticResource ToolButton}" Content="Restart" Width="82" Click="Restart_Click"/>
                    <Button Style="{StaticResource ToolButton}" Content="Stop" Width="66" Click="Stop_Click"/>
                    <Button Style="{StaticResource ToolButton}" Content="Copy" Width="66" Click="Copy_Click"/>
                    <Button Style="{StaticResource ToolButton}" Content="Settings" Width="86" Margin="0" Click="Settings_Click"/>
                </WrapPanel>

                <Grid Grid.Row="1" Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Speed" Foreground="#AEB9C8" VerticalAlignment="Center" FontSize="12" Margin="0,0,10,0"/>
                    <Slider x:Name="SpeedSlider" Grid.Column="1" Minimum="0.6" Maximum="1.8" Value="1"
                            TickFrequency="0.1" IsSnapToTickEnabled="False"
                            ValueChanged="SpeedSlider_ValueChanged"/>
                    <TextBlock x:Name="SpeedText" Grid.Column="2" Text="1.00x" Foreground="#D8E1EC"
                               VerticalAlignment="Center" FontSize="12" FontWeight="SemiBold" Margin="12,0,0,0"/>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
